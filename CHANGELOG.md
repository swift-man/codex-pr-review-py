# 변경 이력

이 프로젝트의 주요 변경 사항을 기록합니다. 버전은 [Semantic Versioning](https://semver.org/)을 따릅니다.

## [Unreleased]

### 수정

- 재기동 직전 operation·instance에 결속한 종료 인계를 확정하여 resume·lease 만료로
  신규 리뷰 접수가 재개되는 경합을 차단합니다.
- 종료 확정 후 서버가 응답 전송과 무관하게 자기 종료를 진행하여 응답 유실이나
  관리 요청 취소로 기존 서버가 영구적으로 503을 반환하던 경로를 수정했습니다.
  관리 스크립트는 기존 PID에 신호를 보내지 않고, 응답 유실 시에도 포트가 비워진 뒤
  새 서버를 검증합니다. 관리 프로세스 취소로 새 기동이 생략되면 수동 복구가 필요합니다.
- 관리 재기동 로그를 append 모드로 열어 기존 서버 종료 로그가 덮어써지지 않도록 수정했습니다.
- 배열·문자열·숫자 등 JSON 객체가 아닌 webhook payload를 400으로 거절합니다.
- macOS Bash 3.2에서 종료 대기 후 listener 목록이 비면 수동 시작 스크립트가
  중단되던 오류를 수정하고, 종료된 listener의 잔여 자식 정리는 유지했습니다.
- 서명이 유효하지만 UTF-8이 손상된 webhook 요청은 500 대신 400으로 거절합니다.

### 추가

- Admin의 모델 저장 후 재기동을 위한 loopback 전용 인증 status/drain/resume/commit-restart API.
  이미 수락한 리뷰의 완료를 기다리며 종료 인계 확정 전 타임아웃·취소·lease 만료 시 intake를 재개합니다.
- 포트 8022의 인증된 단일 drain 프로세스만 종료하는 고정 macOS 재기동 스크립트와
  새 instance 검증, owner-only `.admin-runtime` 잠금·로그를 추가했습니다.
- 신규 요청의 전달 헤더로 loopback peer를 위장하지 못하도록 Uvicorn proxy headers를
  비활성화하고, 전용 secret 설정·초기 수동 재기동·실패 delivery 재전송 절차를 문서화했습니다.

## [0.1.7] - 2026-09-05

### 변경

- 기본 리뷰 fallback 순서를 `gpt-5.6-sol` → `gpt-reserve` → `gpt-5.3-codex-spark`로 변경
- 세 모델 모두 `xhigh` 추론 강도로 시도하도록 운영 예제와 문서를 동기화

## [0.1.6] - 2026-09-02

### 수정

- 큰 저장소의 리뷰 프롬프트가 Codex CLI `turn/start` 1,048,576자 제한을 넘어 실패하던 문제 수정
- 모델 토큰 예산과 CLI 전송 한도를 함께 적용하고, 실행 직전 실제 프롬프트 길이를 재검증

## [0.1.5] - 2026-08-31

### 변경

- 1순위 `gpt-5.6-sol` 리뷰에 Codex CLI 확장 컨텍스트 872,000을 명시하고 입력 예산을 828,400으로 상향
- fallback 모델에는 Sol 컨텍스트 오버라이드를 전달하지 않아 각 모델의 실제 입력 한도를 유지

### 수정

- `xhigh` 추론 강도와 입력 컨텍스트 예산이 연결된 것처럼 보이던 운영 설정을 분리하고, 입력 예산이 1순위 모델의 유효 윈도우를 넘으면 기동 시 거부
- 공백 `GITHUB_APP_SLUG`가 `[bot]` 로그인으로 활성화되어 자기 댓글 제외를 우회하던 경로 차단
- Codex stderr의 쉼표 포함 token footer가 실제 오류 원인을 가리던 진단 문제 수정
- 입력 예산을 생략한 기존 작은 모델 환경이 Sol 정적 기본값 때문에 기동 실패하던 하위 호환성 문제 수정
- 알려진 모델의 명시 컨텍스트가 카탈로그 최대값을 넘어 입력 예산 검증을 우회하지 못하도록 차단
- 운영 예제에 노출된 모든 내장 모델의 기본·최대 컨텍스트를 분리해 동적 예산과 명시 상한 검증에 반영
- 검증된 설정값을 받는 입력 예산 기본 팩터리에 맞춰 Pydantic 최소 버전을 2.10으로 명시

## [0.1.4] - 2026-08-30

### 변경

- 추론 강도 기본값과 허용 타입을 설정 및 Codex CLI 실행 경로가 공유하도록 통합
- `CODEX_REASONING_EFFORT`의 대소문자와 주변 공백을 기동 시점에 정규화하고 `max`·`ultra` 표시를 지원

### 수정

- 지원하지 않는 추론 강도 오타와 알려진 모델·fallback 비호환 조합이 Codex CLI 호출까지 전달되던 문제를 설정 검증 단계에서 차단

## [0.1.3] - 2026-08-30

### 추가

- GitHub 리뷰 footer에 실제 사용 모델과 Codex 추론 강도를 사람이 읽기 쉬운 단일 라벨로 함께 표시

### 변경

- 기본 리뷰 추론 강도를 `high`에서 `xhigh`로 상향하고 로컬 환경 예제와 운영 문서를 동기화

## [0.1.2] - 2026-07-11

### 변경

- 설정과 실행 엔진의 모델 중복 제거 로직을 공통 구현으로 통합

### 수정

- GitHub App private key 소스 누락·중복 또는 공백 입력을 기동 시점에 거부하고 테스트 mock의 반환 타입을 실제 계약과 일치시킴

## [0.1.1] - 2026-07-11

### 변경

- 리뷰 모델 fallback 순서를 `gpt-5.6-sol` → `gpt-5.5` → `gpt-5.3-codex-spark`로 조정

## [0.1.0] - 2026-07-10

### 추가

- GitHub App 웹훅을 받아 PR 전체 코드베이스를 Codex CLI로 검토하는 비동기 리뷰 파이프라인
- 변경 라인에 고정되는 4단계 심각도 리뷰와 병합 차단 이벤트 자동 판정
- 전체 컨텍스트가 모델 입력 예산을 넘을 때 unified patch로 전환하는 diff-only fallback
- 모델 호출 실패 시 순서대로 재시도하는 모델 fallback과 실제 사용 모델 표시
- 다른 리뷰 봇의 의견을 컨텍스트로 수집하고 필요한 경우 답글을 게시하는 리뷰 이력 기능
- 새 커밋에서 해결된 기존 봇 코멘트를 판정하고 스레드를 정리하는 follow-up 처리
- 제한된 동시성, 저장소별 checkout 잠금, 종료 시 진행 중 리뷰 보존을 포함한 운영 안전장치

### 변경

- 기본 리뷰 모델을 `gpt-5.6-sol`로 변경
- 기본 입력 예산을 372,000 토큰 컨텍스트의 95%인 353,400 토큰으로 조정
- 기본 fallback 모델을 `gpt-5.3-codex-spark`로 유지하고 모델별 실패 진단을 강화
- 설정, 로컬 환경 예제, README의 모델 및 입력 예산 안내를 동일한 값으로 통일

### 수정

- 리뷰 게시 직전 PR HEAD를 재검증해 오래된 커밋에 대한 리뷰가 게시되는 문제 방지
- GitHub installation token 만료 시 PR 조회, 리뷰 조회, 리뷰 게시를 새 토큰으로 재시도
- Codex와 Git 하위 프로세스의 timeout 및 취소 시 프로세스를 확실히 종료하고 회수
- 로그, 예외, 진단 코멘트에 포함될 수 있는 토큰과 자격 증명을 재귀적으로 마스킹
- 접두사가 붙거나 일부 따옴표가 손상된 모델 JSON 응답을 제한된 범위에서 복구
- GitHub 422 응답 시 유효한 인라인 코멘트를 보존하고 본문 리뷰로 안전하게 재시도
